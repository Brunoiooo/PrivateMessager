# API network binding
API_BIND_IP=0.0.0.0
API_BIND_PORT=5000

# Optional runtime configuration
POSTGRES_CONNECTION_STRING=Host=localhost;Port=5432;Database=messager;Username=messager;Password=messager
JWT_SIGNING_KEY=replace-with-strong-random-value
